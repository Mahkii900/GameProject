//INITIAL STATE
const initialState = {

}

//ACTION CONSTS


//ACTION BUILDERS


//REDUCER
export default (state = initialState, action) => {
    switch(action.type) {
        default: return state
    }
}